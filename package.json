{
  "name": "agentkit",
  "version": "0.1.0",
  "description": "CLI to discover and adopt Agent Manifesto Kit capabilities",
  "type": "module",
  "bin": {
    "agentkit": "dist/cli.js"
  },
  "files": [
    "dist",
    "collection"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "test": "npm run build && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.19.19",
    "typescript": "^5.6.0"
  }
}
