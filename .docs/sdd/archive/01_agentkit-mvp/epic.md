---
id: epic-agentkit-mvp
title: agentkit CLI MVP
status: done
parent: none
---

## Intent

Ship the kit as an `agentkit` NPM CLI so consumers can discover capabilities and adopt them into a project, placed correctly for their AI provider. (No product PRD predates this epic; it was specified directly. Future product context lives under `product/`.)

## Features

| # | Feature | Status |
|---|---------|--------|
| 01 | `list` command | 🟢 done |
| 02 | `adopt` command + providers | 🟢 done |

## Status rollup

🟢 done — both features implemented, tested (14 `node:test` cases), and verified. See `plan.md` for the technical design and `tasks/` for the implementation units.
